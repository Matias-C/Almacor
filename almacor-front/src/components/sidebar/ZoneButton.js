import "./ZoneButton.css"

function ZoneButton(props) {
    return(

        <div className="zone-btn-cont">
            <h2>{props.zoneButton}</h2>
        </div>

    );
}

export default ZoneButton;